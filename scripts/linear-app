#!/usr/bin/env bash
set -euo pipefail
runtime_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd -P)"
source_repo="${SYMPHONY_PROJECT_ROOT:-${SYMPHONY_SOURCE_REPO:-}}"
if [[ -z "${source_repo//[[:space:]]/}" ]]; then
  common_dir="$(git rev-parse --path-format=absolute --git-common-dir)"
  source_repo="$(cd "$common_dir/.." && pwd -P)"
fi
export SYMPHONY_SOURCE_REPO="$source_repo"
export SYMPHONY_ROOT_DIR="$runtime_root"
: "${SYMPHONY_WORKFLOW_FILE:?Explizite lokale Workflowdatei erforderlich}"
exec "$runtime_root/scripts/mix-runtime" mix "$runtime_root" run --no-compile --no-start "$runtime_root/scripts/linear-app.exs" "$@"
